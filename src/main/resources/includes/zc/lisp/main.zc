@include<lisp/runtime.zc>
@include<lisp/parser.zc>

fn main() {
     val mem = makeAutoMemory(10000*sizeof<Cons>);
     val context = createRuntime(mem);


     # assume this function will be generated by compiler
     evalProgram(context);

     freeRuntimeContext(context);
     freeAutoMemory(mem);

}